describe("AppStack", () => {
  describe("with default props", () => {
    it.todo("propagates tags to all resources");
  });

  describe("with propagateTags disabled", () => {
    it.todo("does not propagate tags to resources");
  });
});
